
package es.bsc.dataclay.paraver;

/** Paraver events. */
public enum ParaverEventType {
	/** Indicates we are exiting a local method. */
	EXIT_LOCAL_METHOD, 
	/** Indicates we are entering a local method. */
	ENTER_LOCAL_METHOD, 
}
